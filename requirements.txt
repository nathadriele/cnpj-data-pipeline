mage-ai==0.7.14 # Version used for development in Mage.ai
boto3==1.28.0 # AWS SDK for Python, used by mage_ai.io.s3
pandas==1.5.3 # Library for data manipulation
requests==2.31.0 # To perform downloads via HTTP
pyyaml==6.0 # Used by ConfigFileLoader to load the io_config.yaml file